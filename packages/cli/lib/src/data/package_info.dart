class PackageInfo {
  const PackageInfo({
    required this.name,
    required this.version,
    required this.description,
  });

  final String name;
  final String version;
  final String description;
}
